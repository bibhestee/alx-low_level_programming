#include "main.h"
/**
* mul - multiplies two integers
* 'a' : to be multiplied by b
* 'b' : to be multiplied by a
* Return: mul(the product)
*/


int mul(int a, int b)
{
	int mut;
	mut = a * b;
	return (mut);
}
