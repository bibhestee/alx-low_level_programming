#include "main.h"

/**
 *
 */

char *leet(char *)
{
	int x = 0;

	while ( s[x] != 0)
	{
		if (s[x] = 'a' || s[x] = 'A')
		{
			s[x] = 4;
		}
		else if(s[x] = 'e' || s[x] = 'E')
		{
			s[x] = 3:
		}
		else if(s[x] = 'o' || s[x] = 'O')
		{
			s[x] = 0;
		}
		else if(s[x] = 't'  || s[x] = 'T')

		{
			s[x] = 7;
		}
		else if (s[x] = 'l' || s[x] = 'L')
		{
			s[x] = 1;
		}
		else
		{
			continue;
		}
			x++;
	}
	return s[];
}

