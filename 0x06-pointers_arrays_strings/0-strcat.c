#include "main.h"

/**
 * _strcat - get two strings
 * @dest: pointer 1
 * @src: pointer 2
 * Return: return concatination of the two pointers
 */

char _strcat(char *dest, char *src)
{
	return (*dest + *src);
}
